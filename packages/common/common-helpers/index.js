import { closeApp, dispatchEvent } from './src/lifecycle/lifecycle'
import { inputTerminalCommand, inputTerminalCommands } from './src/terminal/terminal'
import { getAvailableUpdate } from './src/updates/updates'

export { closeApp, dispatchEvent, getAvailableUpdate, inputTerminalCommand, inputTerminalCommands }
