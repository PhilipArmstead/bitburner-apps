{
  "name": "@bitburner-theme-browser/app",
  "version": "0.0.1",
  "scripts": {
    "start": "workspaces-run --parallel -- npm run start",
    "build": "workspaces-run --parallel -- npm run build",
    "lint": "workspaces-run --parallel -- npm run lint"
  },
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.0.1",
    "eslint": "^8.6.0",
    "eslint-plugin-vue": "^8.2.0",
    "sass": "^1.47.0",
    "vite": "^2.7.10",
    "workspaces-run": "^1.0.1"
  }
}
